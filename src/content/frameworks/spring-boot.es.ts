// engine
import type { QuestionTranslation } from '../../engine/question';

export const translations: Record<string, QuestionTranslation> = {
  'spring-boot-application-annotation-basics': {
    prompt:
      "```java\n@SpringBootApplication\npublic class DemoApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(DemoApplication.class, args);\n    }\n}\n```\n¿Qué combina la única anotación `@SpringBootApplication`?",
    options: {
      a: '`@SpringBootConfiguration` (a su vez meta-anotada con `@Configuration`, así que la clase es fuente de definiciones de beans), `@EnableAutoConfiguration` (permite que Spring Boot configure beans según el classpath y cualquier propiedad que encuentre), y `@ComponentScan` (escanea el paquete de la clase anotada, y sus subpaquetes, en busca de componentes)',
      b: '`@Controller`, `@Service` y `@Repository` — agrupa las tres anotaciones de estereotipo para que cada clase del proyecto se trate automáticamente como las tres a la vez, sin importar lo que esa clase realmente declare ser, convirtiendo todo el código en beans web, de servicio y de persistencia a la vez',
      c: 'Solo `@EnableAutoConfiguration`; `@Configuration` (o `@SpringBootConfiguration`) y `@ComponentScan` todavía hay que agregarlas por separado en la misma clase, o el escaneo de componentes y cualquier método `@Bean` declarado a mano dejan de funcionar en silencio la próxima vez que se reconstruya el contexto',
      d: '`@SpringBootTest`, `@ExtendWith(SpringExtension.class)` y `@AutoConfigureMockMvc` — arma un contexto de aplicación de pruebas completo, con un bean `MockMvc` simulado y reversión transaccional de pruebas, para cada clase anotada con ella, sea de prueba o no',
    },
    explanation:
      '`@SpringBootApplication` está meta-anotada exactamente con estas tres: `@SpringBootConfiguration` (que a su vez está meta-anotada con `@Configuration`), para que la clase también pueda declarar métodos `@Bean`; `@EnableAutoConfiguration`, para que Spring Boot inspeccione el classpath (y cualquier condición `@ConditionalOn...`) y configure beans como un Tomcat embebido o un `DataSource` sin XML; y `@ComponentScan` con raíz en el paquete de la clase anotada, para que las clases `@Component`/`@Service`/`@Repository`/`@Controller` debajo de ese paquete se descubran automáticamente. La opción de "`@Controller`, `@Service` y `@Repository`" confunde las anotaciones de estereotipo (que marcan clases individuales para el escaneo) con lo que hace `@SpringBootApplication` en sí — no convierte retroactivamente a cada clase del proyecto en las tres cosas. La opción de "solo `@EnableAutoConfiguration`" es incorrecta porque `@Configuration`, vía `@SpringBootConfiguration`, y `@ComponentScan` ya están incluidas; agregarlas de nuevo sería redundante, no necesario. La opción de `@SpringBootTest` confunde esto con anotaciones de testing, que son un asunto aparte, opcional, y solo para clases de prueba — `@SpringBootTest` nunca está implícita en `@SpringBootApplication`.',
  },
  'spring-boot-repository-interface-basics': {
    prompt:
      "```java\npublic interface ProductRepository extends JpaRepository<Product, Long> {\n    List<Product> findByNameContainingIgnoreCase(String name);\n}\n```\nNinguna clase del código implementa `ProductRepository`, y sin embargo se puede inyectar con `@Autowired` y usar para llamar a `.save(product)`, `.findById(id)` y `.findByNameContainingIgnoreCase(\"widget\")`. ¿Qué es `ProductRepository`, y de dónde sale su implementación?",
    options: {
      a: 'Es una interfaz de repositorio de Spring Data JPA. Al arrancar, Spring Data busca las interfaces que extienden `JpaRepository` (o similares) y crea un proxy en tiempo de ejecución que implementa los métodos CRUD heredados, además de derivar la consulta de `findByNameContainingIgnoreCase` a partir del propio nombre del método',
      b: 'Es una interfaz de repositorio, pero solo los métodos CRUD heredados de `JpaRepository` (`save`, `findById`, etcétera) funcionan en tiempo de ejecución; un método de consulta derivada como `findByNameContainingIgnoreCase` compila pero lanza `UnsupportedOperationException` al invocarlo, porque Spring Data no puede generar SQL real solo a partir de un nombre de método — eso requiere una anotación `@Query` explícita',
      c: 'Solo funciona porque `Product` implementa `Serializable`; Spring construye la implementación reflejando los getters y setters de `Product` al arrancar, no la propia interfaz `ProductRepository`, y el nombre del método se descarta',
      d: 'La clase `@SpringBootApplication` debe declarar un método `@Bean` que devuelva `new ProductRepositoryImpl()`; el proxy se genera en tiempo de compilación mediante un procesador de anotaciones invocado desde ese método bean, de forma parecida a cómo Lombok genera código a partir de `@Getter`',
    },
    explanation:
      'Las interfaces de repositorio de Spring Data JPA no necesitan una implementación escrita a mano: al arrancar, la infraestructura de Spring Data encuentra cada interfaz que extiende alguna de sus interfaces base (`Repository`, `CrudRepository`, `JpaRepository`, etcétera) dentro de los paquetes escaneados y genera un proxy — respaldado por `SimpleJpaRepository` para los métodos heredados — que se registra como un bean de Spring bajo ese tipo de interfaz. Los métodos de consulta sin cuerpo, como `findByNameContainingIgnoreCase`, se resuelven analizando el nombre del método para construir una consulta JPQL en el arranque (`Containing` se vuelve un `LIKE %...%`, `IgnoreCase` envuelve ambos lados en una comparación sin distinguir mayúsculas) — esa derivación es justo lo que hace que el método funcione sin `@Query`, por lo que la opción que dice que las consultas derivadas lanzan `UnsupportedOperationException` lo tiene al revés. Ningún compilador ni procesador de anotaciones genera una clase real `ProductRepositoryImpl` en tiempo de compilación, lo cual descarta la opción del `@Bean` con `ProductRepositoryImpl`; esto es un proxy en tiempo de ejecución, no código generado. Que la entidad implemente `Serializable` no tiene relación con cómo se construye el proxy del repositorio, lo cual descarta la opción de `Serializable`.',
  },
  'spring-boot-circular-dependency-boot26': {
    prompt:
      "```java\n@Service\npublic class OrderService {\n    @Autowired\n    private PaymentService paymentService;\n}\n\n@Service\npublic class PaymentService {\n    @Autowired\n    private OrderService orderService;\n}\n```\nEste proyecto arranca sin problemas en Spring Boot 2.5. Después de actualizar a Spring Boot 3.x, el arranque falla con `BeanCurrentlyInCreationException`, reportando una referencia circular entre `orderService` y `paymentService`. ¿Qué cambió, y cuál es la solución recomendada?",
    options: {
      a: 'Spring puede resolver una dependencia circular por campo/setter exponiendo una referencia temprana, aún sin terminar, de cada bean mientras el otro se está construyendo — pero desde Boot 2.6, `spring.main.allow-circular-references` es `false` por defecto, así que el ciclo ahora falla de inmediato al arrancar. Volver a poner la propiedad en `true` silencia la excepción, pero no es la solución; hay que romper el ciclo, por ejemplo con `@Lazy` en un campo',
      b: 'Cambiar ambos campos a inyección por constructor — `OrderService(PaymentService paymentService)` y su espejo en `PaymentService` — soluciona el ciclo directamente, ya que la inyección por constructor resuelve las referencias circulares de forma más confiable que la inyección por campo, y es el estilo generalmente recomendado',
      c: 'La excepción significa que `OrderService` y `PaymentService` deben anotarse ambos con `@Primary`; agregar `@Primary` a cualquiera de las dos clases le indica a Spring cuál de dos beans candidatos ambiguos preferir, resolviendo la ambigüedad que dispara la verificación de referencia circular al arrancar el contexto',
      d: 'Anotar una de las clases con `@DependsOn("paymentService")` le indica a Spring que construya e inicialice ese bean por completo antes de que arranque el otro, lo cual rompe la referencia circular aquí sin requerir ningún cambio en cómo se inyecta o construye ninguno de los dos servicios',
    },
    explanation:
      "Las dependencias circulares inyectadas por campo/setter solían funcionar porque Spring instancia cada singleton con su constructor por defecto primero, guarda en caché una referencia temprana a esa instancia todavía no completamente poblada, y recién después realiza la inyección de campos/setters — así que `orderService` y `paymentService` pueden recibir cada uno una referencia a la instancia temprana del otro. (Los ciclos con inyección por constructor son otra historia y nunca han sido resolubles de esta manera, en ninguna versión de Spring, porque un constructor necesita sus argumentos antes de que el objeto exista siquiera). Spring Boot 2.6 cambió el valor por defecto de `spring.main.allow-circular-references` a `false`, así que el mismo ciclo por inyección de campo que funcionaba en silencio en 2.5 ahora falla de inmediato con `BeanCurrentlyInCreationException`. Volver a poner la propiedad en `true` restaura el comportamiento anterior, pero solo vuelve a ocultar un mal olor de diseño; la solución recomendada es eliminar el ciclo, lo más común con `@Lazy` en uno de los dos campos inyectados, para que ese lado solo se resuelva (a través de un proxy) la primera vez que realmente se use, bien después de que ambos beans terminen de construirse. Cambiar ambos campos a inyección por constructor es la respuesta trampa: no arregla este ciclo — lo empeora, porque un ciclo genuino de constructor a constructor nunca puede ser resuelto por Spring (el objeto no puede existir antes que el argumento de su propio constructor), así que falla de forma incondicional en lugar de solo cuando `allow-circular-references` es `false`. La opción de `@Primary` está inventada — `@Primary` resuelve empates entre varios beans *candidatos* para un solo punto de inyección y no tiene relación con los ciclos. La opción de `@DependsOn` también es incorrecta: `@DependsOn` solo controla el *orden* de creación entre beans que no se referencian directamente entre sí; no hace nada para satisfacer la dependencia de campo directa que cada clase sigue declarando, así que el mismo `BeanCurrentlyInCreationException` igual se lanzaría.",
  },
  'spring-boot-valid-requestbody-response': {
    prompt:
      "```java\n@RestController\n@RequestMapping(\"/api/orders\")\npublic class OrderController {\n\n    @PostMapping\n    public ResponseEntity<OrderResponse> create(@Valid @RequestBody CreateOrderRequest request) {\n        Order saved = orderService.create(request);\n        return ResponseEntity.status(HttpStatus.CREATED).body(OrderResponse.from(saved));\n    }\n\n    @GetMapping(\"/{id}\")\n    public ResponseEntity<OrderResponse> getById(@PathVariable Long id,\n                                                  @RequestParam(required = false) Boolean includeItems) {\n        // ...\n    }\n}\n```\n¿Cuáles afirmaciones sobre este controlador son correctas? Selecciona todas las que apliquen.",
    options: {
      a: '`@RequestBody` deserializa el cuerpo JSON en un `CreateOrderRequest` mediante un `HttpMessageConverter` registrado (Jackson por defecto); con `spring-boot-starter-validation` en el classpath, `@Valid` luego ejecuta las restricciones de `CreateOrderRequest`, y una que falle rechaza la petición con `400 Bad Request` (`MethodArgumentNotValidException`) antes de que `create` se ejecute',
      b: '```java\n@PathVariable Long id,\n@RequestParam(required = false) Boolean includeItems\n```\n`id` vincula el segmento `{id}` de la plantilla de URL, mientras que `includeItems` vincula un parámetro opcional de query string (por ejemplo, `?includeItems=true`); una petición que omite `includeItems` lo deja en `null` en lugar de fallar',
      c: 'Devolver `ResponseEntity<OrderResponse>` en lugar de un `OrderResponse` simple es obligatorio para que funcione la serialización JSON — si `create` devolviera en cambio un `OrderResponse` simple, `@RestController` no tendría forma de escribirlo en el cuerpo de la respuesta',
      d: '`@RestController` es la forma abreviada de `@Controller` combinado con `@ResponseBody` aplicado a cada método manejador, así que los valores de retorno se escriben directamente en el cuerpo de la respuesta HTTP mediante conversión de mensajes en lugar de resolverse como el nombre de una vista',
      e: 'Si `CreateOrderRequest` no tiene ninguna anotación de restricción de Bean Validation en ningún campo (sin `@NotNull`, `@Size`, etc.), poner `@Valid` delante de `@RequestBody` hace que la aplicación no arranque, porque no hay nada que el validador pueda revisar',
    },
    explanation:
      "Las afirmaciones sobre `@RequestBody`/`@Valid` y sobre `@PathVariable`/`@RequestParam` describen el comportamiento estándar y bien definido de `@RequestBody`/`@Valid` (incluyendo que `@Valid` no hace nada hasta que un proveedor de Bean Validation, como Hibernate Validator vía `spring-boot-starter-validation`, esté en el classpath) y `@PathVariable`/`@RequestParam`, y son correctas tal como están escritas. La afirmación de `@Controller` más `@ResponseBody` también es correcta: `@RestController` es exactamente `@Controller` + `@ResponseBody`, por eso los métodos manejadores devuelven datos en lugar de un nombre de vista. La afirmación de que `ResponseEntity` es necesario para serializar a JSON es falsa — `@RestController` aplica `@ResponseBody` a cada manejador sin importar el tipo de retorno, así que un `OrderResponse` simple se serializa a JSON con un `200 OK` por defecto sin problema; `ResponseEntity` solo hace falta cuando el método quiere controlar el código de estado, los encabezados, o ambos, de forma explícita (como hace `create` aquí para devolver `201 Created`), no para que la serialización sea posible. La afirmación de que `@Valid` sin restricciones impide el arranque es falsa — `@Valid` sin restricciones en el tipo destino no hace nada: la validación se ejecuta, no encuentra nada que revisar, y la petición sigue adelante; no provoca ni un fallo de arranque ni un error en tiempo de ejecución.",
  },
  'spring-boot-save-vs-merge': {
    prompt:
      "```java\npublic interface ProductRepository extends JpaRepository<Product, Long> {}\n\n@Service\npublic class ProductService {\n    private final ProductRepository repository;\n    // constructor omitted\n\n    public Product createOrUpdate(Product product) {\n        return repository.save(product);\n    }\n}\n```\n`Product.id` está anotado así:\n\n```java\n@Id\n@GeneratedValue(strategy = GenerationType.IDENTITY)\nprivate Long id;\n```\nUn llamador construye `new Product()`, dejando `id` en `null`, y llama a `createOrUpdate`. ¿Qué hace `JpaRepository.save(...)` en ese caso — y en qué se diferenciaría el comportamiento si el llamador en cambio pasara un `Product` con un `id` no nulo que todavía no existe en la base de datos (digamos, un id copiado de otro sistema)?",
    options: {
      a: '`SimpleJpaRepository.save` decide entre insertar y actualizar mediante `isNew()` — un campo `@Version` si existe (`null` significa nueva), si no, el id (`null`, o `0` si es primitivo); `@GeneratedValue` no participa en absoluto. `id == null` significa `persist`, un `INSERT`. Un `id` no nulo sin fila coincidente significa `merge`: un `SELECT` no encuentra nada, y en Hibernate 6.6+ (Boot 3.4/3.5) eso lanza `ObjectOptimisticLockingFailureException`; en versiones ≤ 6.5 insertaba bajo un id nuevo, descartando el tuyo',
      b: '`save` siempre realiza un `INSERT` sin importar el valor de `id`; si ya existe una fila con ese `id`, la base de datos lo rechaza con una violación de clave primaria, así que `save` nunca puede usarse para actualizar una fila existente — hay que agregar un método `update` aparte al repositorio para eso, tal como se hacía en las capas DAO escritas a mano de hace años',
      c: 'Si `save` inserta o actualiza depende únicamente de si la transacción que lo rodea es `@Transactional(readOnly = true)`: una transacción de solo lectura siempre resulta en `merge`, de forma que nada se escribe realmente, y una de escritura siempre resulta en `persist`, sin importar el valor de `id` ni si existe una fila coincidente',
      d: 'Como `Product` usa `GenerationType.IDENTITY`, Hibernate solo puede asignar el id después de insertar la fila, así que llamar a `save` con un `id` no nulo, aún no persistido, lanza `IdentifierGenerationException` de inmediato, antes de que llegue cualquier SQL a la base de datos, sin importar qué versión de Hibernate esté en el classpath',
    },
    explanation:
      "Este es un problema bien conocido porque, en versiones antiguas de Hibernate, ambos caminos terminaban insertando una fila, así que la diferencia era fácil de pasar por alto en una prueba rápida; en Hibernate 6.6+ ya no es nada sutil, porque el camino del id no nulo ahora falla directamente en lugar de duplicar en silencio la fila bajo una nueva clave. `isNew()` — la comprobación en la que se apoya `save` internamente — mira primero `@Version` cuando la entidad lo tiene, y solo recurre al id cuando no lo tiene; `@GeneratedValue` nunca entra en esa decisión, solo controla cómo obtiene su valor un id *nuevo* una vez que se elige `persist`. La opción de \"siempre hace un `INSERT`\" es incorrecta — `save` es el punto de entrada combinado de Spring Data para crear-o-actualizar precisamente para que los llamadores no necesiten un método `update` aparte; enruta a `persist` o `merge` según la comprobación de nuevo/no-nuevo, no siempre a `INSERT`. La opción de `readOnly` está inventada — `readOnly` solo afecta cómo se configura la transacción (por ejemplo, una pista para omitir el dirty checking y, en algunos drivers, marcar la conexión como de solo lectura); no participa en la decisión de persist-vs-merge, y una transacción de solo lectura no convierte en silencio las escrituras en no-ops. La opción de `IdentifierGenerationException` es incorrecta — Hibernate no rechaza un id no nulo de entrada sin importar la versión; usa el valor no nulo del id para decidir que la entidad 'no es nueva' y sigue el camino de `merge` descrito en la respuesta de `isNew()`, cuyo resultado sí depende de la versión de Hibernate en uso.",
  },
  'spring-boot-transactional-self-invocation': {
    prompt:
      "```java\n@Service\npublic class ReportService {\n\n    public void generateMonthlyReport() {\n        // ... build the report ...\n        saveReport();\n    }\n\n    @Transactional\n    public void saveReport() {\n        reportRepository.save(report);\n        auditRepository.save(audit);\n    }\n}\n```\nUn llamador inyecta `ReportService` y llama a `generateMonthlyReport()`. Ambas escrituras de repositorio dentro de `saveReport()` se ejecutan, pero `@Transactional` en `saveReport()` nunca surte efecto: `SimpleJpaRepository.save` es en sí mismo `@Transactional`, así que `reportRepository.save(report)` y `auditRepository.save(audit)` abren y confirman cada una su propia transacción por separado en lugar de compartir una. Si `auditRepository.save(audit)` lanza una excepción, `reportRepository.save(report)` ya se confirmó y no se puede revertir. ¿Por qué `@Transactional` en `saveReport()` nunca surte efecto aquí, y cómo reestructurarías el código para arreglarlo?",
    modelAnswer:
      "Por defecto, Spring implementa `@Transactional` (y `@Cacheable`, `@Async`) con AOP basado en proxies: para un bean basado en clase como este (sin interfaz), Spring crea al arrancar una subclase CGLIB de `ReportService` y registra *ese proxy* como el bean que se inyecta en otros componentes. El proxy sobrescribe `saveReport()` para abrir una transacción, invocar el método real en el objeto de destino subyacente, y confirmar o revertir alrededor de él. Esa interceptación solo ocurre para llamadas que llegan *a través de la referencia al proxy*. Dentro de `generateMonthlyReport()`, la llamada `saveReport()` es en realidad `this.saveReport()` — una llamada virtual de Java normal sobre el objeto de destino sin envolver, hecha desde código que ya está corriendo dentro del propio destino (el proxy ya delegó hacia él para ejecutar `generateMonthlyReport()`). Esa llamada nunca vuelve a salir a través del proxy, así que el advice transaccional que Spring tejió alrededor de `saveReport()` nunca se dispara. Eso no significa que las dos escrituras corran sin ninguna transacción: el propio método `save` de `SimpleJpaRepository` está anotado `@Transactional`, así que tanto `reportRepository.save(report)` como `auditRepository.save(audit)` igual abren y confirman su propia transacción cuando se llaman sin ninguna ya activa. El verdadero defecto es que las dos escrituras ya no comparten *una sola* transacción atómica — si la segunda escritura lanza una excepción, la primera ya se confirmó por su cuenta y no se puede revertir junto con ella. La misma regla de auto-invocación se aplica de forma idéntica a `@Cacheable` y `@Async`. No es del todo cierto que el advice basado en proxy solo pueda alcanzar métodos públicos: desde Spring Framework 6.0, los proxies CGLIB también pueden dar advice a métodos `protected` y de paquete (package-private), aunque los métodos `final` y las auto-invocaciones genuinas siguen fuera del alcance de cualquier proxy, porque un proxy solo puede interceptar llamadas que le llegan desde afuera, nunca una llamada que un bean se hace a sí mismo. El modo AspectJ (weaving en tiempo de compilación o de carga) es el único enfoque que sí puede dar advice a auto-invocaciones y métodos privados, porque reescribe el bytecode de la propia clase en lugar de envolverla en un proxy. Sin cambiar de modo de AOP, la solución es enrutar la llamada a través del proxy en lugar de a través de `this`: la opción más limpia es mover `saveReport()` a su propio bean colaborador y hacer que `ReportService` llame a ese bean; alternativamente, inyectar una autorreferencia (`@Lazy @Autowired private ReportService self;`) y llamar a `self.saveReport()`, o habilitar `@EnableAspectJAutoProxy(exposeProxy = true)` y llamar a `((ReportService) AopContext.currentProxy()).saveReport()`. Extraer un bean colaborador suele ser preferible porque no depende de detalles internos del proxy y mantiene honesta la API pública de la clase sobre qué es transaccional.",
    rubric: [
      'Identifica que @Transactional (como @Cacheable y @Async) se implementa envolviendo el bean en un proxy (una subclase CGLIB aquí, al no haber interfaz), no reescribiendo la clase misma',
      'Indica que llamar a saveReport() como this.saveReport() desde dentro de la misma clase es una auto-invocación que evita el proxy por completo, así que el advice transaccional de saveReport() nunca se ejecuta',
      'Da al menos una solución concreta que enrute la llamada de vuelta a través del proxy: un bean colaborador aparte, una autorreferencia inyectada, o AopContext.currentProxy() con exposeProxy habilitado',
      'Señala que cada escritura del repositorio igual corre en su propia transacción (SimpleJpaRepository.save es en sí mismo @Transactional), así que el verdadero defecto son dos confirmaciones separadas en lugar de una sola transacción atómica compartida',
    ],
    explanation:
      "Este es el problema con `@Transactional` más común en código real, y casi nunca se anuncia como una pregunta sobre proxies — aparece como \"¿por qué no se revirtió esto?\" días después de que el código se escribió y se revisó. La trampa para quienes saben que `@Transactional` existe pero no cómo se implementa es asumir que las anotaciones actúan sobre el método sin importar desde dónde se llame, en lugar de actuar solo sobre las llamadas que cruzan el límite del proxy.\n\n**Dilo en voz alta:** \"`@Transactional` solo se dispara en llamadas que pasan por el proxy del bean, así que `this.saveReport()` lo evita y cada escritura del repositorio se confirma por su cuenta. Movería `saveReport()` a otro bean, o inyectaría una autorreferencia, para que la llamada vuelva a salir por el proxy.\"",
  },
  'spring-boot-controlleradvice-problemdetail': {
    prompt:
      "```java\n@RestControllerAdvice\npublic class ApiExceptionHandler {\n\n    @ExceptionHandler(ProductNotFoundException.class)\n    public ProblemDetail handleNotFound(ProductNotFoundException ex) {\n        ProblemDetail problem = ProblemDetail.forStatusAndDetail(HttpStatus.NOT_FOUND, ex.getMessage());\n        problem.setProperty(\"productId\", ex.getProductId());\n        return problem;\n    }\n}\n```\nSpring Framework 6 agregó soporte de primera clase para `ProblemDetail` (RFC 7807/9457). ¿Qué afirmación describe mejor lo que logra este manejador comparado con construir a mano un DTO de error propio?",
    options: {
      a: '`ProblemDetail` es una representación integrada del formato "problem+json", con los campos estándar (`type`, `title`, `status`, `detail`, `instance`) más miembros de extensión arbitrarios mediante `setProperty`; devolverlo desde un `@ExceptionHandler` hace que Spring MVC lo serialice como `application/problem+json`, tomando el estado HTTP del propio objeto, dándole a cada endpoint una forma de error consistente y basada en un estándar en lugar de un DTO propio por equipo',
      b: 'Configurar `spring.mvc.problemdetails.enabled=true` en `application.properties` haría innecesario este `@ExceptionHandler`, ya que el manejo integrado de Spring Boot ya convierte automáticamente cada excepción, incluidas las excepciones propias de la aplicación como `ProductNotFoundException`, en un `ProblemDetail`',
      c: 'Como `@RestControllerAdvice` está declarada aquí sin atributos `basePackages` ni `assignableTypes`, `ApiExceptionHandler` solo se aplica a controladores del mismo paquete que `ApiExceptionHandler`; manejar excepciones lanzadas por controladores de otros paquetes requiere un `@RestControllerAdvice` por cada paquete',
      d: '`ProblemDetail` solo funciona cuando el tipo de retorno del método manejador es exactamente `ResponseEntity<ProblemDetail>`; si el método devuelve un objeto `ProblemDetail` sin envolver, como hace este, la maquinaria de conversión de mensajes de Spring MVC lo ignora en silencio y el cliente recibe en su lugar un cuerpo de respuesta `200 OK` vacío',
    },
    explanation:
      "`ProblemDetail` estandariza lo que antes era un DTO de error improvisado por aplicación: modela directamente los campos de RFC 7807/9457, `setProperty` cubre extensiones específicas del dominio como `productId`, y Spring MVC tiene soporte dedicado tanto para serializarlo como `application/problem+json` como para derivar de `ProblemDetail.getStatus()` el estado HTTP de la respuesta cuando el método devuelve un `ProblemDetail` sin envolver en lugar de un `ResponseEntity` — justo por eso la opción que exige `ResponseEntity<ProblemDetail>` es incorrecta; envolverlo en `ResponseEntity` solo hace falta cuando hay que fijar encabezados adicionales, no para que funcionen el estado o el cuerpo. La opción que confía en `spring.mvc.problemdetails.enabled=true` exagera lo que realmente hace esa propiedad: hace que las excepciones *propias* de Spring Boot (`NoHandlerFoundException`, `HttpRequestMethodNotSupportedException`, y similares) se rendericen como `ProblemDetail` mediante un `ResponseEntityExceptionHandler` por defecto, pero no tiene idea de que `ProductNotFoundException` existe — una excepción propia del dominio sigue necesitando exactamente el manejador que se muestra aquí. La opción que limita el advice a su propio paquete está al revés: sin `basePackages`/`assignableTypes`/`annotations` que la acoten, `@RestControllerAdvice` se aplica globalmente, a todos los controladores de la aplicación, no solo a las clases de su propio paquete.\n\n**Dilo en voz alta:** \"`ProblemDetail` es la forma de error RFC 9457 (antes 7807) integrada en Spring, y devolverla directamente desde un `@ExceptionHandler` alcanza — Spring MVC lee el estado del propio objeto y lo serializa como `application/problem+json` sin necesitar un envoltorio `ResponseEntity`.\"",
  },
  'spring-boot-n-plus-one-lazy-loading': {
    prompt:
      "```java\n@Entity\npublic class Author {\n    @Id @GeneratedValue\n    private Long id;\n    private String name;\n\n    @OneToMany(mappedBy = \"author\")\n    private List<Book> books;\n}\n\n@Transactional(readOnly = true)\npublic List<AuthorSummary> listAuthorsWithBookCount() {\n    List<Author> authors = authorRepository.findAll();\n    return authors.stream()\n        .map(a -> new AuthorSummary(a.getName(), a.getBooks().size()))\n        .toList();\n}\n```\nCon 200 autores, este método dispara 201 sentencias SQL, y — si la colección se accediera en cambio fuera de una transacción, o con `spring.jpa.open-in-view=false` y ninguna sesión activa en el momento en que corre `.getBooks()` — podría lanzar `LazyInitializationException` en su lugar. Explica ambos problemas y cómo arreglarías cada uno.",
    modelAnswer:
      "Las 201 consultas son un N+1 clásico: `authorRepository.findAll()` emite un único `SELECT` para los autores, y `@OneToMany` usa `FetchType.LAZY` por defecto, así que `books` queda como un proxy/colección sin inicializar en cada `Author`. La primera vez que `a.getBooks()` se ejecuta dentro del stream, Hibernate dispara un `SELECT ... WHERE author_id = ?` aparte para los libros de ese autor — una consulta extra por autor, así que 1 + 200 = 201 en total, creciendo de forma lineal con la cantidad de autores. La solución es dejar de cargar los libros de a uno por autor: agregar `JOIN FETCH` a un método de consulta JPQL (`SELECT a FROM Author a LEFT JOIN FETCH a.books`), o anotar el método del repositorio con `@EntityGraph(attributePaths = \"books\")`, cualquiera de los dos convierte esto en un único join SQL en lugar de N selects adicionales. Para este caso de uso en particular — donde solo hace falta el *conteo* de libros, no los libros en sí — una solución todavía mejor es no cargar las entidades `Book` en absoluto y en cambio consultar una proyección, por ejemplo un `@Query(\"SELECT new com.example.AuthorBookCount(a.name, COUNT(b)) FROM Author a LEFT JOIN a.books b GROUP BY a.id, a.name\")` que devuelva un `record AuthorBookCount(String name, long bookCount) {}`. El join tiene que seguir siendo un `LEFT JOIN`, no un join interno, o los autores con cero libros quedarían fuera del resultado en lugar de mostrar un conteo de cero; esto evita traer a memoria filas de `Book` potencialmente grandes solo para llamar a `.size()`.\n\n`LazyInitializationException` es un problema aparte, aunque relacionado, y `open-in-view` no es lo que causa el N+1 de arriba — `listAuthorsWithBookCount` ya es `@Transactional`, así que hay una sesión de Hibernate abierta durante todo el método sin importar el valor de `open-in-view`, y las 201 consultas ocurren dentro de esa misma transacción de todas formas. Un proxy o colección con carga perezosa necesita una sesión de Hibernate abierta (el contexto de persistencia activo) para ejecutar su consulta de carga perezosa la primera vez que se toca; la excepción aparece cuando `.getBooks()` se llama *después* de que esa sesión ya se cerró — por ejemplo, desde un llamador sin `@Transactional`, o más tarde mientras Jackson serializa la respuesta fuera de cualquier transacción. El valor por defecto de Spring Boot, `spring.jpa.open-in-view=true`, evita esa excepción en particular manteniendo la sesión de Hibernate abierta durante el resto de la petición HTTP (incluyendo la serialización/el renderizado de la vista), así que el acceso con carga perezosa en cualquier punto más abajo de la pila tiene éxito en silencio en lugar de lanzar una excepción — pero lo logra dejando que ese mismo tipo de consultas N+1 se disparen de forma invisible fuera de cualquier límite `@Transactional` explícito, y manteniendo una conexión de base de datos reservada más tiempo del necesario. La solución recomendada es deshabilitar `open-in-view` explícitamente y asegurarse de que cada asociación que un caso de uso necesita — incluyendo cualquier cosa que se toque durante la serialización — se cargue dentro del propio método `@Transactional` dueño de la operación, mediante `JOIN FETCH`/`@EntityGraph` o una proyección, para que el límite de la transacción y el límite de la consulta sean el mismo límite.",
    rubric: [
      'Identifica la causa del N+1: @OneToMany usa LAZY por defecto, así que cada llamada a a.getBooks() en el bucle dispara su propio SELECT, dando 1+N consultas en total',
      'Propone JOIN FETCH o @EntityGraph como la solución que colapsa el N+1 en una sola consulta, e idealmente también menciona una proyección con LEFT JOIN + GROUP BY / DTO para un caso de solo conteo como este',
      'Explica LazyInitializationException como un proxy/colección con carga perezosa que necesita una sesión de Hibernate abierta que ya no está disponible en el momento en que se accede',
      'Indica que open-in-view mantiene la sesión de Hibernate abierta durante el resto de la petición HTTP, de modo que LazyInitializationException queda oculta y las consultas con forma de N+1 igual pueden dispararse fuera de la capa de servicio (por ejemplo, durante la serialización) — y que esto enmascara el problema en lugar de arreglarlo, no que cause el N+1 dentro del método ya @Transactional',
      'Recomienda deshabilitar open-in-view y cargar dentro del propio método @Transactional todo lo que un caso de uso necesita',
    ],
    explanation:
      "Este escenario combina a propósito dos hechos relacionados pero distintos de nivel senior: el N+1 es un error de rendimiento que igual funciona, mientras que `LazyInitializationException` es un error funcional que falla de forma ruidosa — y `open-in-view` queda en el medio, convirtiendo el segundo en una versión silenciosa y costosa del primero en lugar de arreglarlo de verdad.\n\n**Dilo en voz alta:** \"Las 201 consultas son N+1 por el `@OneToMany` con carga perezosa que se toca por autor dentro de un bucle, y se arregla con `JOIN FETCH` o `@EntityGraph`. `open-in-view` solo oculta `LazyInitializationException` manteniendo la sesión abierta más allá de la transacción, así que lo desactivaría y cargaría lo que necesito dentro del método `@Transactional`.\"",
  },
};
