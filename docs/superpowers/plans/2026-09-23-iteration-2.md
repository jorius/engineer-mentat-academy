# Iteration 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply Jose's first-round feedback: company-agnostic content, Spanish for the UI and every question, readable list summaries and kind labels, a correct progress bar, highlighted code blocks, and editor preferences.

**Architecture:** Same codebase as `docs/superpowers/plans/2026-09-23-engineer-mentat-academy.md`. UI strings move to i18next (en/es JSON, same libraries as jorius.github.io). Question translations live beside each subject file as `<subject>.es.ts` exporting `translations: Record<questionId, QuestionTranslation>`, merged by the registry for the active locale and validated by the content test. The layout rework is NOT in this plan; it follows the mockup session.

**Tech Stack additions (exact pins):** i18next 25.8.0, react-i18next 16.5.4, i18next-browser-languagedetector 8.2.0, rehype-highlight 7.0.2, highlight.js 11.12.0.

**Spec:** `docs/superpowers/specs/2026-09-23-engineer-mentat-academy-design.md` (still binding) plus this plan's task text.

## Global Constraints

Same as the first plan: TypeScript strict, explicit return types, labeled import groups, no barrel files, no commented-out code, no eslint-disable comments, exact pins, npm only, commit subjects with a capitalized infinitive verb and no period, every commit ending with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`, never push, never touch `.superpowers/`. Branch: `feature/academy-v1`. Every task ends with `npm run lint`, `npx vitest run`, `npm run build` green.

---

### Task 1: Company-agnostic content

**Files:** every `src/content/**/*.ts` that mentions the former employer name, `src/content/content.test.ts` (if it names the tag), `docs/superpowers/specs/2026-09-23-engineer-mentat-academy-design.md`, `docs/superpowers/plans/2026-09-23-engineer-mentat-academy.md`, `README.md` (if it mentions the tag).

- [ ] Rewrite every prompt/explanation sentence that names the former employer (or any employer) into neutral interviewer language: "<Employer> asks for X" → "Interviewers ask for X"; "the <employer>'s list" → "the classic senior list". Keep the technical content byte-identical otherwise.
- [ ] Rename the original employer-derived tag to `core-25` everywhere (content, README, docs); the `source` enum value derived from the original PDF stays unchanged in the schema but rename its label wherever displayed (none today).
- [ ] In the spec and plan, replace the interview-specific sentences (deadline line naming the employer, the PDF sentence) with neutral wording ("a senior JavaScript/full-stack interview"); keep the local path lines because they document where the repo lives.
- [ ] Verify with a case-insensitive recursive search for the former employer name across `src docs README.md` returning nothing, and the full gate.
- [ ] Commit: `Remove employer references from questions and docs`.

### Task 2: Progress bar shows progress

**Files:** `src/engine/registry.ts` (+ test), `src/pages/Home.tsx`, `src/pages/Browse.tsx`, `src/pages/BrowseDomain.tsx`, `src/components/primitives/ProgressBar.tsx` (if a second tone is needed).

- [ ] Add `progress: number` (attempted/total, 0 when total is 0) to `Summary`; keep `mastery`.
- [ ] Bars render `summary.progress`; next to each bar print "N/M attempted · mastery P%" (mastery hidden when attempted is 0). Home keeps its per-domain cards.
- [ ] Tests: `registry.test.ts` covers `progress`; `Browse.test.tsx` asserts the bar's `aria-valuenow` equals the attempted fraction for a store with one attempt.
- [ ] Commit: `Fix the progress bars to show attempted questions`.

### Task 3: Plain-text question summaries

**Files:** `src/utils/questionSummary.ts` (+ test), `src/pages/BrowseSubject.tsx`, `src/pages/Review.tsx`, `src/pages/Mock.tsx`.

- [ ] `questionSummary(question, max = 90): string`: drop fenced code blocks (``` ... ```), take the first non-empty remaining line, strip inline Markdown (backticks, `**`, `*`, `_`, links to their text), collapse whitespace, cut at `max` on a word boundary with an ellipsis. If nothing remains (prompt was only code), fall back to `"<Kind label>: <topic name>"` using the taxonomy.
- [ ] Use it in every list that shows a prompt excerpt.
- [ ] Commit: `Add plain-text summaries for question lists`.

### Task 4: Readable kind and level labels

**Files:** `src/engine/labels.ts` (+ test), `src/components/primitives/Badge.tsx`, `src/pages/BrowseSubject.tsx`, `src/pages/Mock.tsx`, `src/components/question/QuestionView.tsx`.

- [ ] `KIND_LABELS: Record<Kind, { label: string; hint: string }>` — single: "Single choice" / "Pick the one correct option"; multi: "Multiple choice" / "Select every correct option"; predict: "Predict output" / "Type what the program prints"; code: "Write code" / "Implement `solution` to pass the hidden tests"; fix: "Fix the code" / "Repair the starter until the tests pass"; sql: "SQL query" / "Write a query against the seeded database"; open: "Explain" / "Answer out loud, then self-score against the rubric". `LEVEL_LABELS` similarly (Junior/Mid/Senior with a one-line hint). Labels are plain constants now; Task 7 moves them into the locale files.
- [ ] `Badge` accepts `title` and renders it as a native tooltip. Every chip and filter checkbox uses the label; the QuestionView header shows the label plus the hint under it in small text.
- [ ] Commit: `Add readable labels for question kinds and levels`.

### Task 5: Syntax highlighting in Markdown

**Files:** `package.json`, `src/components/common/Markdown.tsx` (+ test), `src/index.css`.

- [ ] Add `rehype-highlight@7.0.2` and `highlight.js@11.12.0`. Configure `rehypeHighlight` with `languages` limited to javascript, typescript, sql, json, bash, and `detect: false`.
- [ ] Ship a small highlight.js theme in `src/index.css` scoped to `.md pre code` with light and dark tokens (use the same hues as CodeMirror's oneDark for dark and a github-like set for light). Do not import a full highlight.js CSS file.
- [ ] Test: rendering a ```ts fence yields `span.hljs-keyword` elements.
- [ ] Commit: `Add syntax highlighting to Markdown code blocks`.

### Task 6: Editor and appearance preferences

**Files:** `src/engine/preferences.ts` (+ test), `src/hooks/usePreferences.ts`, `src/pages/Settings.tsx` (+ test), `src/components/common/CodeEditor.tsx`, `src/contexts/ThemeContext.tsx`, `tailwind.config.js`, `src/index.css`, `src/App.tsx`.

- [ ] Preferences store (`ema:prefs:v1`, same try/catch discipline as progress): `{ accent: 'spice' | 'sky' | 'emerald' | 'violet' | 'rose'; editorFont: 'jetbrains' | 'fira' | 'system'; editorFontSize: 12..20; tabSize: 2 | 4 | 8; indentWithTabs: boolean; maxAttempts: 1 | 2 | 3 | 'unlimited' }`. `maxAttempts` is stored now and consumed by the layout rework later.
- [ ] Accent: replace hard-coded `spice-*` classes with a Tailwind color backed by CSS variables (`accent: { 500: 'rgb(var(--accent-500) / <alpha-value>)', ... }`); `ThemeProvider` sets the variables on `<html>` from the preference.
- [ ] CodeEditor reads font family/size (inline style on `.cm-editor`), `EditorState.tabSize`, `indentUnit`, and `indentWithTab` keymap.
- [ ] Settings gets an "Editor" card (font, size, tab size, tabs/spaces with a live preview editor) and an "Appearance" card (accent swatches; theme toggle stays in the header); a "Practice" card with max attempts (label it "used by the upcoming retry flow").
- [ ] Tests: store round trip and defaults; Settings changes persist.
- [ ] Commit: `Add editor and appearance preferences`.

### Task 7: UI internationalization (en/es)

**Files:** `package.json`, `src/i18n/index.ts`, `src/i18n/locales/en.json`, `src/i18n/locales/es.json`, `src/main.tsx`, `src/components/common/Layout.tsx`, every component and page with user-facing text, `src/engine/labels.ts` (labels become keys).

- [ ] Same setup as jorius.github.io (i18next + react-i18next + browser language detector, `ema:lang` in localStorage, fallback en). Language toggle (EN/ES) in the header next to the theme toggle.
- [ ] Every user-facing string goes through `t()`; both JSON files complete; Spanish is natural Latin-American technical Spanish (keep code identifiers in English).
- [ ] A test asserts the two locale files have identical key sets.
- [ ] Commit: `Add Spanish and English interface translations`.

### Task 8: Question translation model

**Files:** `src/engine/question.ts`, `src/engine/registry.ts` (+ tests), `src/content/content.test.ts`, `src/hooks/useQuestionBank.ts`, `src/components/question/*.tsx`, `src/pages/*.tsx` where prompts/explanations render.

- [ ] `QuestionTranslation = { prompt: string; explanation: string; options?: Record<optionId, string>; modelAnswer?: string; rubric?: string[]; tests?: never }` (code, tests, schema, answer keys are never translated). Translation files: `src/content/<domain>/<subject>.es.ts` exporting `translations: Record<string, QuestionTranslation>`; the glob for questions must exclude `*.es.ts`.
- [ ] `localizeQuestion(question, locale, translations): Question` returns the merged question (English fallback per field). `useQuestionBank` exposes the localized list for the active i18next language; grading still runs on the canonical question (option ids and answer keys are locale-independent).
- [ ] Content test: every translation key is an existing question id; option translations reference existing option ids; rubric arrays match length; no translation for a `sql` schema or `code`.
- [ ] Commit: `Add per-question translations with English fallback`.

### Task 9: Spanish content

**Files:** one `src/content/<domain>/<subject>.es.ts` per subject (nine agents, one per domain, in parallel; controller commits per domain).

- [ ] Translate prompt, explanation, options (text only), modelAnswer, rubric for every question. Keep code identifiers, language keywords and the `**Say this out loud:**` marker as `**Dilo en voz alta:**`. Do not translate anything inside code fences.
- [ ] Each agent runs the content test filtered by its subjects; the controller runs the whole gate and commits `Add the Spanish <domain> translations`.

---

## Self-review notes
- Task 4's labels are constants first and become locale keys in Task 7 so Tasks 3–5 can ship before the i18n sweep.
- Task 6's `maxAttempts` is stored but unused until the layout rework, on purpose: the mockup session decides the retry UX.
- Order: 1 and 2 in parallel (disjoint files), then 3 → 4 → 5 → 6 → 7 → 8 → 9.
